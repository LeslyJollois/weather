apiVersion: apps/v1
kind: Deployment
metadata:
  name: stg-go-page-subscription
spec:
  replicas: 3
  selector:
    matchLabels:
      app: stg-go-page-subscription
  template:
    metadata:
      labels:
        app: stg-go-page-subscription
    spec:
      containers:
      - name: stg-go-page-subscription
        image: europe-west1-docker.pkg.dev/weather-436309/stg-go-page-subscription/go-lead_event_subscription:latest
        env:
        - name: ENV_VAR_FILE
          value: ".env"
